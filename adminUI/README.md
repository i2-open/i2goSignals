
  # Administrative Interface for Event Streams (Copy)

  This is a code bundle for Administrative Interface for Event Streams (Copy). The original project is available at https://www.figma.com/design/JKDIMP7zVPzKOJhjd7sDS5/Administrative-Interface-for-Event-Streams--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  