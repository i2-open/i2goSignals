# Name of the themes directory is the themes ID: gosignals-mui
# Keep overrides minimal; rely on Keycloak default templates.

parent=keycloak.v2
types=login

# Register our stylesheet (resolved from resources/css)
styles=css/theme.css

# Optional metadata
locales=en
displayName=GoSignals MUI
description=Login theme styled to match the GoSignals admin Material-UI look and feel
